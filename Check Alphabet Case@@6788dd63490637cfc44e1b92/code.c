#include <stdio.h>
int main() 
{
    char ch;
    scanf("%c",ch);
    if(ch==Uppercase)
    {
        printf("Uppercase");
    }
    else if (ch==Lowercase)
    {
        printf("Lowercase");
    }
    else
    {
        printf("Not an alphabet");
    }

}