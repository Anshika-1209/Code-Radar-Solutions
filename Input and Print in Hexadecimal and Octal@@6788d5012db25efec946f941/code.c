#include <stdio.h>
int main() {
    int num;
    scanf("%d", &num);
    
    // Print the number in hexadecimal
    printf("Hexadecimal: %X\n", num);
    
    // Print the number in octal
    printf("Octal: %o\n", num);
    
    return 0;
}
