#include <stdio.h>
int main() {
    int N;
    int sum;
    scanf("%d", &N);
    // Calculate the sum using the formula
    sum = N * (N + 1) / 2;
    // Print the result
    printf("%d\n", N, sum);
  return 0;
}


